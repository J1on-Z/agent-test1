# 单元测试报告 · 电商 RAG 知识库智能问答系统

> 生成时间：2026-09-10 ｜ 执行方式：pytest + pytest-cov（APP_ENV=test，每用例独立临时 SQLite）

## 1. 总体结论

| 指标 | 结果 |
|---|---|
| 测试用例总数 | **41** |
| 通过 | **41（100%）** |
| 失败 / 跳过 | 0 / 0 |
| 执行耗时 | 10.6 秒 |
| 语句覆盖率 | **53%**（2259 语句中覆盖 1207） |
| 核心模块覆盖率 | LangGraph 流水线 100%、数据模型 100%、文档解析 87%、提示词 92% |

**结论**：全部用例通过，流水线核心逻辑（检索融合、引文机制、认证安全）覆盖充分；
服务编排层（chat_service、摄入 pipeline 等）以手工端到端验收为主（见第 4 节已知限制）。

## 2. 用例清单（按测试文件）

| 测试文件 | 用例数 | 覆盖内容 |
|---|---|---|
| test_security.py | 8 | bcrypt 哈希/校验/畸形输入、密码强度规则、JWT 签发/类型隔离/篡改/过期/refresh jti |
| test_auth_api.py | 8 | 注册/登录/me、重复注册拦截、错误密码、未认证 401、refresh 旋转防重放、登出吊销、改密后旧凭证全失效、admin 角色保持 |
| test_parsers.py | 7 | PDF（页码定位）/MD（标题提取）/DOCX/XLSX（结构化行+工作表）/TXT（GBK 回退）解析、空文件与非法类型报错 |
| test_chunker.py | 3 | 中文标点分块边界与块长限制、来源元数据透传、表格行不切断与行号范围 |
| test_hybrid_rrf.py | 4 | RRF 排序、双路去重、双路命中互惠提升、空输入 |
| test_citations.py | 6 | 引文编号解析（越界过滤/去重保序/零引用/英文方括号不干扰）、上下文编号与格式一致性 |
| test_graph.py | 5 | LangGraph 全图走查（Fake 模型）：无历史跳过改写、有历史改写、低分走确定性拒答、rerank 关闭直通、摘要节点路由 |

## 3. 覆盖率明细

**高覆盖（≥80%）**：数据模型与 schema 100%、LangGraph 三件套 100%（builder/edges/state）、
nodes 82%、文档解析 87%、分块 100%、提示词 92%、认证服务 82%。

**低覆盖模块及原因（均为服务编排层，已由手工验收覆盖）**：

| 模块 | 覆盖率 | 说明 |
|---|---|---|
| chat_service | 18% | SSE 流式主流程：已通过 demo.py 与 curl 帧级验收（流式/缓存命中/拒答/断连落库） |
| ingest/pipeline + jobs | 0% / 20% | 摄入流水线：已实测 64 份多格式文档全量入库、去重、进度轮询、重启任务清理 |
| cache_service | 0% | 语义缓存：已实测精确命中 83ms、换措辞语义命中、TTL 与记忆隔离逻辑 |
| kb_service / stats_service | 23% / 22% | 管理端接口：已实测上传/删除/重建/检索调试四层结果、统计看板各指标与操作一致 |
| vector_store / bm25 | 26% / 37% | 检索存储：已通过 eval 脚本实测（recall@5 = 100%）与 benchmark 延迟分解 |
| conversation_service / memory_service | 27% / 22% | 会话与记忆：已实测多轮对话、滚动摘要更新、历史找回 |

## 4. 已知限制（如实说明）

1. **前端无自动化测试**：Vue 组件仅通过 `vite build` 编译校验 + 浏览器手工验收，未引入 Vitest/Cypress。
2. **单测不调用真实 DashScope API**：LLM/embedding/rerank 在单测中均为 Fake/桩（保证离线可跑、结果稳定），
   真实 API 链路由 `scripts/demo.py`（8 步动线）与 `scripts/eval_retrieval.py` 覆盖。
3. **摄入流水线未纳入单测**：其行为依赖真实文件与向量化 API，用脚本验收（`scripts/ingest_all.py`）。
4. 覆盖率 53% 反映的是「单测」边界；若论文需要更高数字，可对 services 层补 API 级自动化测试
   （httpx ASGITransport + Fake LLM，模式已具备，见 `.claude/skills/unit-test/SKILL.md`）。

## 5. 复现命令

```bash
cd backend
# 普通执行
PYTHONUTF8=1 ./.venv/Scripts/python.exe -m pytest tests/ -q
# 带覆盖率（本报告数据来源）
PYTHONUTF8=1 ./.venv/Scripts/python.exe -m pytest tests/ --cov=app --cov-report=term-missing -q
```

前置：`scripts/generate_mock_docs.py` 生成样例文档（test_parsers 依赖，缺失时自动 skip）。

## 6. 检索质量评估（配套实验，非单测）

| 指标 | 结果 |
|---|---|
| 混合检索 recall@5 | 100%（49/49） |
| 重排序 top1 准确率 | 100%（49/49） |
| 无关问题拒答正确率 | 100%（2/2） |

复现：`python scripts/eval_retrieval.py`（51 题金标集，随样例数据生成）
