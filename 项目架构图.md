# 电商 RAG 知识库智能问答系统 —— 架构图

> 基于 LangChain + LangGraph + FastAPI + Vue3 的企业级 RAG 问答系统
> 生成日期：2026-09-11

## 一、系统总体架构（三层）

```
┌──────────────────────────────────────────────────────────────────┐
│                        浏 览 器（用户端）                          │
│           普通用户：注册/登录 · 多会话问答 · 历史找回              │
│           管理员(admin)：知识库管理 · 统计看板                     │
└──────────────────────────┬───────────────────────────────────────┘
                           │  HTTP + SSE 流式（JWT 双 Token）
┌──────────────────────────▼───────────────────────────────────────┐
│              Vue3 + Element Plus + Pinia + Vite（:5173）          │
│  ┌─────────┐ ┌──────────┐ ┌─────────────┐ ┌───────────┐         │
│  │ Login / │ │ ChatView │ │ KbManageView│ │ StatsView │         │
│  │ Register│ │ 会话侧栏   │ │ 上传/摄入    │ │ 统计看板   │         │
│  └─────────┘ └────┬─────┘ └─────────────┘ └───────────┘         │
│   router 路由守卫 │  useChatStream(SSE)  api/* axios 拦截器       │
└───────────────────┼──────────────────────────────────────────────┘
                    │  Vite proxy /api → :8000
┌───────────────────▼──────────────────────────────────────────────┐
│                FastAPI + Uvicorn（:8000，单进程）                  │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ API 层    auth · chat · conversations · kb · stats · health │ │
│  │ 横切      JWT认证(access 30min/refresh 7d 旋转吊销)          │ │
│  │           slowapi 限流(IP + 用户 + 全局三层)                  │ │
│  │           request_logs 埋点(分节点耗时/TTFT/缓存标记)         │ │
│  ├─────────────────────────────────────────────────────────────┤ │
│  │ 核心层    LangGraph RAG 流水线（见二）                        │ │
│  │           chat_service(SSE 编排) · memory_service(滚动摘要)  │ │
│  │           cache_service(语义缓存两级)                        │ │
│  │           ingest 流水线(parsers→chunker→embedding→入库)     │ │
│  ├─────────────────────────────────────────────────────────────┤ │
│  │ 检索层    vector_store(numpy 矩阵) · bm25 · hybrid(RRF)      │ │
│  │           reranker(DashScope 原生 rerank)                    │ │
│  └─────────────────────────────────────────────────────────────┘ │
└───────┬────────────────────┬──────────────────────┬───────────────┘
        │ SQLAlchemy(async)  │ LLM/Embedding/Rerank │ numpy 内存矩阵
┌───────▼────────┐ ┌─────────▼──────────────────┐ ┌───────▼────────┐
│  SQLite(app.db)│ │  阿里百炼 DashScope         │ │ 向量检索(自研)  │
│ users           │ │  工作空间专属端点            │ │ chunk向量 float32│
│ conversations   │ │  · qwen3.7-flash (LLM)     │ │ 余弦相似度 TopK │
│ messages        │ │  · qwen3.7-text-embedding │ │ (替代 chromadb) │
│ documents/chunks│ │    (1024 维)               │ │                 │
│ vector_embeddings│ │  · qwen3.7-text-rerank    │ │                 │
│ ingest_jobs     │ │  信号量限并发(3) + 流式降级 │ │                 │
│ semantic_cache_ │ └────────────────────────────┘ └─────────────────┘
│  entries        │
│ refresh_tokens  │
│ request_logs    │
└─────────────────┘
```

## 二、LangGraph 问答流水线（核心）

```
                    START
                      │
          ┌───────────┴─────────────┐
          │ route_after_start        │ 缓存命中(精确/向量相似+记忆hash)？
          ▼                          ▼
   [rewrite_query]            直接返回缓存答案
   上下文感知改写                  │
   (仅缓存未命中时)               │
          │                      │
          ▼                      │
      [retrieve]                 │
   ① 改写结果 → embedding API    │
   ② numpy 向量检索 TopK ─┐      │
   ③ BM25 关键词检索 ─────┼─ RRF 融合
          │               │
          ├─── route_after_retrieve ── rerank 开关？
          ▼
      [rerank]  DashScope 重排序 → TopN
          │
          ▼
   [context_gate]  相关度 ≥ 阈值？
          │                    │
        是 ▼                   ▼ 否
    [generate]          [no_context_answer]
  带引用流式生成           无答案话术
  (SSE: meta→token*       (同样流式返回)
   →thinking→
   citations→done)
          │
          ├─ route_after_generate ─ 多轮对话？
          ▼
   [update_summary]  滚动摘要写回 memory（记忆感知缓存 key）
          │
         END
```

## 三、数据模型（10 张表）

| 表 | 用途 | 关键设计 |
|---|---|---|
| users / refresh_tokens | 账号 / 双 token 旋转吊销 | bcrypt 哈希，admin 角色隔离 |
| conversations / messages | 多用户多会话隔离 | 按 user_id 隔离，message_count 计数 |
| documents / chunks / vector_embeddings | 知识库三层结构 | 文档→分块→float32 向量（SQLite 持久化） |
| ingest_jobs | 摄入任务进度 | 异步摄入，前端轮询进度 |
| semantic_cache_entries | 两级语义缓存 | 精确文本 + 向量余弦，记忆感知 key，TTL 24h |
| request_logs | 性能埋点 | 分节点耗时 / TTFT / 缓存标记（压测交叉验证用） |

## 四、请求时序（一次问答）

```
前端 ChatView ──POST /api/chat(SSE)──▶ chat_service.stream_chat
   │                                      │ 短 session: 校验用户/会话
   │                                      │ 保存用户消息 → request_logs
   │                                      ├─▶ cache_service.lookup(问题,记忆hash)
   │◀─ ①meta(会话id/消息id)───────────────│   命中 → 直接回放
   │                                      ├─▶ graph 执行（见二，长 session 在节点内自管）
   │◀─ ②token*（LLM 流式增量）───────────│      · LLM 调用经信号量(3)排队
   │◀─ ③thinking（思考片段）─────────────│      · 15s 心跳帧保活
   │◀─ ④citations（引用来源/相似度）─────│   完成后: 保存 AI 消息 + 缓存写入
   │◀─ ⑤done────────────────────────────│
```

## 五、关键架构决策（毕设答辩可用）

1. **自研 numpy 向量检索替代 chromadb** —— chroma-hnswlib 在 Python 3.14/Windows 下 upsert 段错误（已最小复现），改为 SQLite 持久化向量 + numpy 内存矩阵余弦相似度；生产替换 Milvus/Qdrant。
2. **LangGraph 无 checkpointer** —— 每次请求独立建图，多轮上下文由 chat_service 从 SQLite 组装，满足「不同时间登录找回历史对话」的持久化需求。
3. **短生命周期 DB session** —— 修复压测发现的连接池耗尽问题（请求级 session 跨 LLM 生成期持有连接）。
4. **RAG 增强点**：查询改写、RRF 混合检索、重排序、相关度门槛、滚动摘要记忆、两级语义缓存、SSE 带引用流式输出。

## 六、后端模块结构

```
backend/app/
├── main.py                  # FastAPI 应用入口（中间件、路由挂载）
├── config.py                # pydantic-settings 配置（.env 加载）
├── database.py              # SQLAlchemy async 引擎 + 连接池
├── dependencies.py          # 认证依赖注入（get_current_user）
├── api/                     # 路由层：auth / chat / conversations / kb / stats / health
├── core/                    # 横切：security(JWT/bcrypt) / rate_limit / exceptions / logging
├── models/                  # ORM 10 张表
├── schemas/                 # Pydantic 请求/响应模型
├── graph/                   # LangGraph：state / nodes / edges / builder
├── services/
│   ├── chat_service.py      # SSE 编排（短 session 设计）
│   ├── cache_service.py     # 两级语义缓存
│   ├── memory_service.py    # 滚动摘要记忆
│   ├── conversation_service.py / auth_service.py / kb_service.py / stats_service.py
│   ├── llm/                 # qwen(ChatOpenAI→DashScope) / embeddings / prompts
│   ├── retrieval/           # vector_store(numpy) / bm25 / hybrid(RRF) / reranker
│   └── ingest/              # parsers / chunker / pipeline / jobs
└── utils/                   # sse / hashing / text

frontend/src/
├── api/                     # axios 封装 + SSE 客户端
├── stores/                  # Pinia：auth / chat
├── router/                  # 路由守卫（登录态 + admin 角色）
├── views/                   # 登录/注册/聊天/个人中心 + admin(知识库管理/统计看板)
├── components/chat/         # 会话侧栏 / 消息气泡(引用卡片) / 输入框
└── composables/useChatStream.js   # SSE 流式状态机
```
