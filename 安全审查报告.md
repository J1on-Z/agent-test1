# 安全审查报告 · 电商 RAG 知识库智能问答系统

> 审查时间：2026-09-10 ｜ 审查范围：后端 FastAPI/全部服务层、前端 Vue、配置文件、依赖清单、Git 历史
> 审查方式：`.claude/skills/security-audit` 技能（正则扫描 + 文件确认 + pip-audit + npm audit）

## 1. 结论摘要

| 严重级别 | 数量 | 项目 |
|---|---|---|
| 🔴 高危 | **0** | — |
| 🟡 中危 | **1** | echarts 依赖 XSS 漏洞 |
| 🟢 低危/建议 | **3** | chromadb 残留依赖含 CVE、异常信息外泄、SECRET_KEY 默认兜底 |

**总体评价**：未发现高危漏洞。认证、授权、输入校验、文件上传等关键面均有正确防护；
发现的问题集中在**依赖管理**与**纵深防御细节**，均可低成本修复。

---

## 2. 详细发现

### 🟡 [中危] echarts < 6.1.0 存在 XSS 漏洞

- **位置**：`frontend/package.json`（echarts ^5.6.0，实际安装 <6.1.0）；使用点 `frontend/src/views/admin/StatsView.vue`
- **证据**（npm audit 输出）：
  ```
  echarts  <6.1.0
  Severity: moderate
  Apache ECharts has a cross-site scripting (XSS) vulnerability
  - https://github.com/advisories/GHSA-fgmj-fm8m-jvvx
  ```
- **影响**：ECharts 特定渲染路径可被注入脚本。本项目中图表数据**全部来自自有数据库的数值聚合**（问答数、token 数、延迟），无法注入任意字符串，实际可利用性低；但依赖本身已标注漏洞，且看板未来若展示用户可控文本（如用户名）将直接暴露。
- **修复建议**：`npm install echarts@^6.1.0`（注意大版本升级需回归验证 StatsView 图表渲染）。

### 🟢 [低危] chromadb 残留安装含 4 个已知 CVE

- **位置**：`backend/.venv`（不在 `requirements.txt`，代码零引用）
- **证据**（pip-audit 输出）：
  ```
  Found 4 known vulnerabilities in 1 package
  chromadb 1.5.9  PYSEC-2026-311 / CVE-2026-45830 / CVE-2026-45833 / CVE-2026-45831
  ```
- **背景**：项目因 chromadb 在 Python 3.14/Windows 下段错误，已改用自研 numpy 向量检索并从依赖清单移除（`requirements.txt` 第 35 行注释说明）。
- **影响**：代码无引用路径，当前**不可被触发**；但残留包扩大了供应链攻击面（若未来误 import 或打包分发，将带入已知漏洞）。
- **修复建议**：`.venv/Scripts/pip uninstall chromadb langchain-chroma onnxruntime`（清理后重跑测试确认无回归）。

### 🟢 [低危] 聊天异常向客户端返回原始错误文本

- **位置**：`backend/app/services/chat_service.py:337`
- **证据**：
  ```python
  else:
      yield sse.sse_error("chat_error", f"回答生成失败：{e}")
  ```
- **影响**：异常原文可能包含内部信息（上游 API 端点域名、请求 ID、库内部路径）。仅登录用户可见，且模型未开通类错误已单独处理为友好文案，故风险有限。
- **修复建议**：改为固定文案 + 服务端日志记录详情：
  ```python
  logger.exception("流式问答异常", exc_info=e)   # 详情仅落服务端日志
  yield sse.sse_error("chat_error", "回答生成失败，请稍后重试或联系管理员")
  ```

### 🟢 [低危] SECRET_KEY 存在弱默认兜底值

- **位置**：`backend/app/config.py:28` → `secret_key: str = "dev-secret-change-me"`
- **影响**：当前 `.env` 已配置 64 位随机串（合规）；但若他人克隆仓库后未创建 `.env` 直接运行，JWT 将使用**源码中公开可见的密钥**签发，任何人可伪造任意用户（含管理员）的 token。
- **修复建议**：生产环境改为启动时校验——`app_env != "dev"` 且密钥仍为默认值时直接拒绝启动（fail-fast）。

---

## 3. 合规项确认（扫描通过的安全实践）

| 检查面 | 结果 | 依据 |
|---|---|---|
| 代码硬编码密钥/密码 | ✅ 零命中 | 全量正则扫描 `*.py/*.js/*.vue`，密钥统一走 `settings.dashscope_api_key` |
| `.env` 版本控制保护 | ✅ 合规 | `git check-ignore` 命中 `.gitignore:10`；`git log --all -- "*.env"` 无历史提交记录 |
| `.env.example` 占位符 | ✅ 合规 | 仅含 `sk-xxxxxxxx`、`please-change-me-...` 等占位值 |
| SQL 注入 | ✅ 零风险 | 全部走 SQLAlchemy ORM/参数绑定，无 f-string 拼接 SQL；`text()` 仅用于 `SELECT 1` 健康检查 |
| 命令注入 | ✅ 零风险 | 无 `os.system`/`subprocess`/`eval`/`exec` |
| 反序列化 | ✅ 零风险 | 无 `pickle`/`yaml.load`/`marshal` |
| 前端 XSS | ✅ 已防护 | 唯一 `v-html`（MessageBubble.vue:21）经 `renderMarkdown()` → DOMPurify 白名单净化，且 markdown-it `html:false` 禁用原始 HTML |
| JWT 算法固定 | ✅ 合规 | `pyjwt.decode(..., algorithms=[ALGORITHM])` 显式指定，免疫 alg=none 攻击 |
| 密码存储 | ✅ 合规 | bcrypt（rounds 可配置，默认 12），无 md5/sha1 |
| 越权（IDOR） | ✅ 已防护 | 会话/消息/导出等全部经 `_get_owned_conversation` 校验 `user_id` 归属后返回 |
| 管理接口权限 | ✅ 全覆盖 | `app/api/kb.py`、`app/api/stats.py` 共 16 个端点全部声明 `Depends(require_admin)` |
| CORS 配置 | ✅ 合规 | 白名单限定 localhost:5173，**非** `*`；`allow_credentials` 与白名单同用 |
| 限流 | ✅ 已启用 | 全局 120/min、聊天 10/min、登录/注册 5/min（slowapi） |
| 文件上传 | ✅ 已防护 | 扩展名白名单 + 大小上限（20MB）+ **uuid 重命名落盘**（防路径穿越与重名覆盖） |
| 错误处理 | ✅ 合规 | API 层全局异常处理器返回通用文案（`app/core/exceptions.py`），不暴露堆栈 |

## 4. 已接受的风险（业务需求，附缓解措施）

| 项 | 理由 | 缓解措施 |
|---|---|---|
| `admin/123456` 默认管理员 | 毕设演示硬性要求 | 仅监听 127.0.0.1；登录限流 5 次/分；支持页面改密；README 提示公网部署前必改 |
| 单机无 HTTPS | 本地演示环境 | README 说明生产需反向代理 + TLS |
| 前端 token 存 localStorage | 项目规模下的常见取舍 | XSS 面已用 DOMPurify 收敛；生产可改 HttpOnly Cookie |

## 5. 未覆盖范围（如实说明）

1. **未做渗透测试**：本次为静态代码审查 + 依赖扫描，未进行运行时攻击模拟。
2. **未审计第三方依赖源码**：仅比对公开漏洞库（pip-audit / npm audit）。
3. **前端 npm audit 需指定官方源**：npmmirror 镜像未实现审计接口，命令为
   `npm audit --registry=https://registry.npmjs.org`。
4. **LLM 提示词注入未评估**：RAG 场景下的间接提示词注入（知识库文档含恶意指令）
   未纳入本次范围，如需可补充测试。

## 6. 复现命令

```bash
# 技能化执行（推荐）
#   对 Claude 说：「执行安全审查」→ 自动按 .claude/skills/security-audit 全流程扫描

# 手动复现关键项
cd backend && .venv/Scripts/pip-audit.exe                      # Python 依赖漏洞
cd frontend && npm audit --omit=dev --registry=https://registry.npmjs.org   # 前端依赖漏洞
git check-ignore -v backend/.env                               # 密钥文件忽略确认
git log --all --full-history --oneline -- "*.env"              # 历史泄露检查
```
