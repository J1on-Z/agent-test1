# 电商 RAG 知识库智能问答系统 —— 简历项目报告（含答辩材料）

> 技术栈：LangChain / LangGraph + FastAPI + Vue3 / Element Plus + SQLite + 阿里百炼 DashScope
> 更新日期：2026-09-20

---

## 零、简历速写版（可直接粘贴进简历「项目经历」栏）

**项目名**：企业级电商 RAG 知识库智能问答系统（独立开发）

- 基于 LangChain/LangGraph + FastAPI + Vue3 构建企业级 RAG 问答系统，实现多用户多会话隔离、带引用片段流式问答、知识库管理与 admin 权限隔离
- 设计 8 节点检索增强流水线：查询改写 → 向量 + BM25 混合检索（RRF 融合）→ 重排序 → 相关度门槛 → 带引用流式生成 → 滚动摘要记忆
- 自研 numpy 向量检索（替代在 Python 3.14/Windows 下段错误的 chromadb）与两级语义缓存，缓存命中响应约 100ms，较未命中链路提速 30-60 倍
- 实现扫描件 PDF 自动 OCR 回退：无文字层页面经 PyMuPDF 渲染后由多模态模型逐页识别，实测 19 页扫描审计报告识别 16847 字符入库，检索 top1 命中分数 0.9992
- 主导 100 并发压力测试并逐层定位瓶颈：修复 LLM 信号量未接入、请求级 DB session 连接池耗尽问题，常态 100 人在线 0% 失败、9 RPS
- 交付 41 个单元测试、51 条金标离线检索评测（recall@5/拒答率）、四场景压测报告与安全审查报告（0 高危）

---

## 一、项目简介

面向电商客服场景的企业级知识库问答系统：商品参数、退换货政策、物流规则等知识散落在 PDF/Word/Excel 中，客服人工查资料平均 5-10 分钟且答案不一致。本系统通过 RAG 让大模型**只依据企业知识库回答**，秒级响应、答案带引用可追溯、知识更新即时生效。

**核心数字（实测）**：65 份文档 / 123 分块入库（含 19 页扫描件 OCR 识别）；100 人在线常态 0% 失败、9.02 RPS；缓存命中 ~100ms vs 未命中 3-6s；41 单测全通过；安全审查 0 高危。

---

## 二、系统架构（架构图）

### 2.1 总体三层架构

```
┌──────────────────────────────────────────────────────────────────┐
│                        浏 览 器（用户端）                          │
│           普通用户：注册/登录 · 多会话问答 · 历史找回              │
│           管理员(admin)：知识库管理 · 统计看板                     │
└──────────────────────────┬───────────────────────────────────────┘
                           │  HTTP + SSE 流式（JWT 双 Token）
┌──────────────────────────▼───────────────────────────────────────┐
│              Vue3 + Element Plus + Pinia + Vite（:5173）          │
│  ┌─────────┐ ┌──────────┐ ┌─────────────┐ ┌───────────┐         │
│  │ Login / │ │ ChatView │ │ KbManageView│ │ StatsView │         │
│  │ Register│ │ 会话侧栏   │ │ 上传/摄入    │ │ 统计看板   │         │
│  └─────────┘ └────┬─────┘ └─────────────┘ └───────────┘         │
│   router 路由守卫 │  useChatStream(SSE)  api/* axios 拦截器       │
└───────────────────┼──────────────────────────────────────────────┘
                    │  Vite proxy /api → :8000
┌───────────────────▼──────────────────────────────────────────────┐
│                FastAPI + Uvicorn（:8000，单进程）                  │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │ API 层    auth · chat · conversations · kb · stats · health │ │
│  │ 横切      JWT认证(access 30min/refresh 7d 旋转吊销)          │ │
│  │           slowapi 限流(IP + 用户 + 全局三层)                  │ │
│  │           request_logs 埋点(分节点耗时/TTFT/缓存标记)         │ │
│  ├─────────────────────────────────────────────────────────────┤ │
│  │ 核心层    LangGraph RAG 流水线（见 2.2）                      │ │
│  │           chat_service(SSE 编排) · memory_service(滚动摘要)  │ │
│  │           cache_service(语义缓存两级)                        │ │
│  │           ingest 流水线(parsers→OCR回退→chunker→embedding→入库)│ │
│  ├─────────────────────────────────────────────────────────────┤ │
│  │ 检索层    vector_store(numpy 矩阵) · bm25 · hybrid(RRF)      │ │
│  │           reranker(DashScope 原生 rerank)                    │ │
│  └─────────────────────────────────────────────────────────────┘ │
└───────┬────────────────────┬──────────────────────┬───────────────┘
        │ SQLAlchemy(async)  │ LLM/Embedding/Rerank │ numpy 内存矩阵
┌───────▼────────┐ ┌─────────▼──────────────────┐ ┌───────▼────────┐
│  SQLite(app.db)│ │  阿里百炼 DashScope         │ │ 向量检索(自研)  │
│ users           │ │  工作空间专属端点            │ │ chunk向量 float32│
│ conversations   │ │  · qwen3.7/3.8-flash (LLM) │ │ 余弦相似度 TopK │
│ messages        │ │  · qwen3.7-text-embedding │ │ (替代 chromadb) │
│ documents/chunks│ │    (1024 维)               │ │                 │
│ vector_embeddings│ │  · qwen3.7-text-rerank    │ │                 │
│ ingest_jobs     │ │  · 多模态 OCR(扫描件识别)   │ │                 │
│ semantic_cache_ │ │  信号量限并发(3) + 流式降级 │ │                 │
│  entries        │ └────────────────────────────┘ └─────────────────┘
│ refresh_tokens  │
│ request_logs    │
└─────────────────┘
```

### 2.2 LangGraph 问答流水线（核心）

```
                    START
                      │
          ┌───────────┴─────────────┐
          │ route_after_start        │ 缓存命中(精确/向量相似+记忆hash)？
          ▼                          ▼
   [rewrite_query]            直接返回缓存答案
   上下文感知改写                  │
   (仅缓存未命中时)               │
          │                      │
          ▼                      │
      [retrieve]                 │
   ① 改写结果 → embedding API    │
   ② numpy 向量检索 TopK ─┐      │
   ③ BM25 关键词检索 ─────┼─ RRF 融合
          │               │
          ├─── route_after_retrieve ── rerank 开关？
          ▼
      [rerank]  DashScope 重排序 → TopN
          │
          ▼
   [context_gate]  相关度 ≥ 阈值？
          │                    │
        是 ▼                   ▼ 否
    [generate]          [no_context_answer]
  带引用流式生成           无答案话术
  (SSE: meta→token*       (同样流式返回)
   →thinking→
   citations→done)
          │
          ├─ route_after_generate ─ 多轮对话？
          ▼
   [update_summary]  滚动摘要写回 memory（记忆感知缓存 key）
          │
         END
```

### 2.3 数据模型（10 张表）

| 表 | 用途 | 关键设计 |
|---|---|---|
| users / refresh_tokens | 账号 / 双 token 旋转吊销 | bcrypt 哈希，admin 角色隔离 |
| conversations / messages | 多用户多会话隔离 | 按 user_id 隔离，message_count 计数 |
| documents / chunks / vector_embeddings | 知识库三层结构 | 文档→分块→float32 向量（SQLite 持久化） |
| ingest_jobs | 摄入任务进度 | 异步摄入，前端轮询进度 |
| semantic_cache_entries | 两级语义缓存 | 精确文本 + 向量余弦，记忆感知 key，TTL 24h |
| request_logs | 性能埋点 | 分节点耗时 / TTFT / 缓存标记（压测交叉验证用） |

### 2.4 关键架构决策

1. **自研 numpy 向量检索替代 chromadb** —— chroma-hnswlib 在 Python 3.14/Windows 下 upsert 段错误（已最小复现），改为 SQLite 持久化向量 + numpy 内存矩阵余弦相似度；生产替换 Milvus/Qdrant。
2. **LangGraph 无 checkpointer** —— 每次请求独立建图，多轮上下文由 chat_service 从 SQLite 组装，满足「不同时间登录找回历史对话」的持久化需求。
3. **短生命周期 DB session** —— 修复压测发现的连接池耗尽问题（请求级 session 跨 LLM 生成期持有连接）。
4. **RAG 增强点**：查询改写、RRF 混合检索、重排序、相关度门槛、滚动摘要记忆、两级语义缓存、SSE 带引用流式输出。
5. **扫描件 PDF 自动 OCR 回退** —— 无文字层页面由 PyMuPDF 渲染后交多模态模型逐页识别（默认 qwen3.7-flash，实测支持图片输入）；有文字层页面仍走 pypdf 零成本路径；50 页上限防大文件产生失控费用。

---

## 三、业务反推技术（选型因果链）

### 3.1 业务需求 → 技术选型 → 验证结果（完整映射）

| # | 业务需求 | 由此产生的技术约束 | 技术选型 | 实测验证 |
|---|---|---|---|---|
| 1 | 浏览器知识库管理 | 管理界面 + 文档解析 + 异步摄入 | Vue3 + Element Plus；parsers(PDF/docx/md/xlsx，扫描件自动 OCR 回退) → chunker → ingest_jobs 异步任务 | 65 份文档摄入成功（含 19 页扫描件 OCR），前端轮询进度 |
| 2 | 带引用片段的问答 | **答案必须来自企业知识** → RAG 而非纯 LLM；引用可展示 → 流式带 citations | LangGraph 编排 + SSE 事件流（meta→token*→citations→done） | 引用卡片前端渲染，单测覆盖引用格式 |
| 3 | 多用户多会话隔离 | 多租户硬隔离、并发安全 | SQL 层 user_id 过滤 + SQLite WAL + 短 session | 103 用户 3391 会话互不串扰 |
| 4 | 历史会话找回 | 持久化 + 多轮上下文组装 | SQLite 持久化消息；LangGraph 无 checkpointer，上下文由 DB 组装 | 不同时间登录可恢复会话 |
| 5 | 注册登录改密 | 密码安全 + 会话有效期 | bcrypt 哈希 + JWT 双 token（30min/7d 旋转吊销） | 41 单测含认证安全用例 |
| 6 | admin/123456 权限隔离 | 角色区分 + 越权防护 | User.role 字段 + 路由守卫 + 接口层角色校验 | 普通用户访问管理接口被拒 |
| 7 | 企业级性能优化 | 秒级响应 + 省钱 + 抗并发 | 两级语义缓存 + LLM 信号量(3) + 限流自保护 + request_logs 埋点 | 缓存命中 ~100ms vs 未命中 3-6s；100 人在线 0% 失败 |
| 8 | 附加功能 | 差异化亮点 | 查询改写、混合检索 RRF、重排序、相关度门槛、滚动摘要记忆、统计看板、压测 | 51 条金标离线评测 + 4 场景压测报告 |

### 3.2 五条关键因果链

**① 业务要「可追溯的答案」→ 逼出 RAG → 逼出混合检索 → 逼出自研向量库**

```
客服回答必须引用原文（合规）
  → 纯 LLM 会幻觉，必须 RAG
  → RAG 需要向量检索，但关键词问题（"iPhone15"）向量会漏
  → 向量 + BM25 混合检索 + RRF 融合
  → chromadb 在 Python3.14/Windows 段错误（已最小复现）
  → 业务不能停，自研 numpy 矩阵检索（70 chunks 规模足够快）
```

**② 业务要「秒级 + 省钱」→ 逼出语义缓存 → 逼出记忆服务**

```
100 人同时用，每问都调 LLM 又慢又花钱（3-6s，按量计费）
  → 加缓存
  → 但多轮对话里同一句话在不同上下文下答案不同 → 不能简单按文本缓存
  → 先做滚动摘要记忆（memory_service），用「记忆 hash」做缓存 key
  → 两级缓存：精确文本 + 向量相似度
  → 实测命中 ~100ms，热点问题 0 成本
```

**③ 业务要「7×24 + 100 人」→ 压测逼出架构修正**

```
100 并发压测发现：端到端 TTFT p50 20.4s，但 LLM 生成 p50 只有 1.1s
  → 17 秒等在哪？逐层排查 → SQLite 写锁串行化 + 请求级 session 持有连接
  → 重构为短生命周期独立 session（chat_service/cache_service）
  → 常态 100 人在线 0% 失败、9 RPS
  → 剩余瓶颈（写锁）是单机 SQLite 天花板，生产方案已写进报告
```

**④ 业务要「错误答案不能发出去」→ 逼出相关度门槛**

```
客服场景答错 = 客诉 + 合规风险
  → LLM 低相关度时会硬编
  → 加 context_gate：检索分数低于阈值 → no_context_answer 拒答话术
  → 不给 LLM 编造的机会（金标集含 2 条拒答题专门验证）
```

**⑤ 业务要「扫描件也能入库」→ 逼出多模态 OCR 回退**

```
用户上传 19 页扫描版审计报告 → pypdf 提取不到任何文字 → 摄入失败
  → 纸质/扫描文档是客服知识的重要来源，必须能进知识库
  → 实测发现 qwen3.7-flash 支持图片输入（多模态，中文识别准确）
  → PyMuPDF 渲染页面 → 逐页识别 → 页码定位保留（引用仍可定位「第 N 页」）
  → 有文字层的 PDF 仍走零成本 pypdf 路径，OCR 只兜底
  → 实测识别 16847 字符入库，检索 top1 rerank 分数 0.9992
```

### 3.3 一句话总结（答辩开场/收尾可用）

> **我的技术选型没有一项是「因为流行」：业务要可追溯 → RAG；要便宜快 → 缓存；要安全 → JWT+bcrypt；要单机可部署 → SQLite+numpy；要中文好且合规 → 百炼。每一步都是业务约束倒逼出来的，且每一层都有测试数据证明它生效。**

---

## 四、答辩八问

### Q1 真实业务背景是什么？为什么做，不做有什么问题

**场景**：电商企业（星辰电商）的客服团队需要高频回答商品类问题——商品参数、退换货政策、物流规则、优惠活动等。这些知识散落在 PDF 手册、Word 政策文档、Excel 价格表、FAQ 里。

**不做的痛点**：

| 痛点 | 后果 |
|---|---|
| 客服查一份资料平均 5-10 分钟 | 用户等待不耐烦 → 流失 |
| 答案靠人背，新客服培训 2-4 周 | 人员流动率高，培训成本反复支出 |
| 人工回答不一致，政策更新后旧答案还在用 | 客诉、合规风险 |
| 只能工作时段人工服务 | 无法 7×24 |

**为什么做**：RAG 让 LLM **只依据企业知识库回答**（带引用、可追溯），把「查资料→组织答案」从分钟级压缩到秒级，同时解决一致性和培训问题。

> ⚠️ 诚实标注：本系统的业务数据是**脚本生成的仿真电商文档**（毕设性质），痛点与收益推演基于行业公开常识，不是真实企业运营数据。

### Q2 业务数据是什么？每天多少请求？多少文档？数据从哪来

**数据从哪来**：`scripts/generate_mock_docs.py` 生成 64 份仿真文档（商品手册 PDF、政策 docx、FAQ md、价格表 xlsx），另有 51 条金标问答 `golden_qa.json` 用于评测。

**现库内实测规模**（压测后的模拟运营数据）：

| 数据 | 数量 |
|---|---|
| 文档 / 分块 / 向量 | 65 / 123 / 123 |
| 用户 / 会话 / 消息 | 103 / 3392 / 14718 |
| 性能埋点 request_logs | 5917 条 |

**请求量（压测实测，不是真实业务量）**：
- 100 人在线常态使用：**9.02 RPS、0% 失败**，持续 5 分钟稳定
- 按 8 小时工作日折算 ≈ **26 万次/天**的吞吐上限
- 缓存命中响应 ~100ms，未命中 3-6s

**生产假设推演**（答辩时这么讲）：中型电商 50 人客服团队 × 日均 80 问 ≈ 4000 问/天，数百份文档——系统实测容量完全覆盖。

### Q3 为什么这么选技术？

| 选型 | 理由 |
|---|---|
| LangChain + **LangGraph** | 流程编排可视化，条件路由（改写开关/门槛判断）天然适合 RAG 多分支；无 checkpointer 设计让历史由 SQLite 持久化 |
| **FastAPI** | 原生 async、SSE 流式、自动 API 文档 |
| **Vue3 + Element Plus** | 企业级组件库，快速交付完整管理界面 |
| **SQLite**（async SQLAlchemy） | 零运维单机部署，WAL + busy_timeout；生产可平滑换 Postgres |
| **自研 numpy 向量检索** | chromadb 在 Python 3.14/Windows 下段错误（已最小复现），70 维块规模下 numpy 足够快；生产换 Milvus/Qdrant |
| **扫描件 OCR**（PyMuPDF + 多模态模型） | 无文字层 PDF 自动渲染识别（默认 qwen3.7-flash，实测支持图片输入），纯文本 PDF 保持零成本路径 |
| **阿里百炼 DashScope** | 国内合规直连、免 GPU 部署、qwen3.7 系列中文效果好、按量付费便宜 |
| **JWT 双 token + bcrypt + slowapi** | 认证安全三件套 |

一句话总结：**编排选 LangGraph、服务选 FastAPI、界面选 Element Plus、存储选 SQLite，原则是「单机可跑、可演示、可平滑升级」**。

### Q4 系统边界：什么交给 LLM，什么绝对不能

**交给 LLM 的（纯语言任务）**：
- 查询改写（把口语/指代改成检索友好的查询）
- 答案组织与生成（**只基于检索到的 chunk**，逐条带引用）
- 滚动摘要（记忆压缩）
- 排序理解（rerank 打分）

**绝对不能交给 LLM 的**：
- **认证/授权/多租户隔离**——user_id 过滤在 SQL 层硬隔离，LLM 碰不到别的用户数据
- **限流与密码**——slowapi、bcrypt 在代码层
- **数据库写入**——消息、缓存、日志全部由代码直接写
- **事实裁定**——低相关度时 `context_gate` 直接走 `no_context_answer` 拒答，**不给 LLM 编造的机会**
- **执行操作**——系统无 tool/agent 机制，LLM 只能输出文本，不能触发任何动作

一句话：**LLM 是「会说话的检索结果整理器」，不是决策者**。

### Q5 失败怎么办？超时

**分层容错，全部已实现**：

| 层 | 机制 |
|---|---|
| 网络 | httpx 超时 + tenacity 重试 |
| LLM | 流式失败自动降级为单次 ainvoke；403 → 友好提示（模型未开通）；信号量(3)排队防击穿 |
| Embedding | 限速时摄入任务自动排队，前端轮询进度 |
| 文档解析 | 扫描件无文字层自动回退多模态 OCR；单页失败重试、超页数上限明确报错 |
| 数据库 | WAL + busy_timeout 15s；短 session 防连接池耗尽 |
| 限流 | 全局 120/min，超限返回 429 自保护 |
| 连接保活 | SSE 15s 心跳帧；失败发 error 事件，**用户消息已先落库，不丢数据** |
| 降级 | 缓存命中不依赖 LLM——上游故障时热点问题仍可回答 |

**诚实的边界**：100 人突发时 95% 失败（压测已定位：SQLite 写锁串行化是单机架构天花板）。这是已知问题、已写入压测报告，生产方案是多进程 + Postgres + LLM 池扩容——答辩时主动讲出来反而是加分项。

### Q6 怎么评测？怎么证明系统变好了

**四层证据链，每一层都有可运行的工具**：

1. **单元测试**：41 个用例全通过（认证安全/分块/RRF 融合/图路由/引用格式）
2. **离线检索评测** `eval_retrieval.py`：51 条金标问答，测 **recall@5、rerank 后 top1 准确率、拒答率**——可对比「纯向量 vs 混合检索」「rerank 开/关」，量化证明每项优化带来的提升
3. **在线指标**：request_logs 埋点记录每次请求的分节点耗时、TTFT、缓存标记——实测缓存命中 ~100ms vs 未命中 3-6s，**30-60 倍加速**有数据支撑
4. **压测 + 安全审查**：4 场景压测报告、安全审查 0 高危

**「变好了」的证明逻辑**：每项优化（混合检索、rerank、缓存、相关度门槛）都有独立的评测指标或前后对比数据。

> ⚠️ 诚实标注：目前没有 LLM-as-judge 的端到端生成质量评测——可作为「后续工作」主动提出。

### Q7 线上问题怎么定位？模型/RAG/业务系统

**靠三层可观测性，一分钟定位到层**：

- **`/api/health`**：database、vector_store、dashscope 三个子系统**独立上报 ok 状态** → 先判断哪层挂了
- **request_logs**：每次请求记录各节点耗时 + TTFT + 缓存标记 → 慢在哪层一目了然

**定位决策树**（压测实战验证过）：

| 现象 | 定位 |
|---|---|
| 报错 / 429 / 401 | **业务系统**（认证、限流、DB） |
| TTFT 慢但各节点都快 | **排队层**（DB 写锁/信号量）——压测实测案例：generate 节点 p50 仅 1.1s，端到端 TTFT p50 却 20.4s → 定位到 SQLite 写串行化 |
| retrieve 节点慢 | embedding API |
| 答案质量差 | 看 citations 与门槛：是检索问题（召回/阈值）还是模型问题（改写/生成） |
| dashscope 403 | 模型未开通（health 直接提示，无需查日志） |

### Q8 最终给业务带来了什么？

**实测数据（系统真实测出）**：100 人同时在线 0% 失败、9 RPS；缓存命中 30-60 倍加速。

**收益推演公式（假设参数可调，答辩时说明这是估算）**：

| 维度 | 计算 |
|---|---|
| **时间** | 人工查资料 5-10 min/问 → 秒级；假设日均 4000 问 × 每问省 4 min = **每天省 267 小时 ≈ 33 人日** |
| **钱** | qwen3.7-flash 每问成本 <¥0.05（缓存命中直接 0 成本）；客服人力按 5-8k/人/月计，等效**年省数十万** |
| **培训** | 2-4 周 → 1-2 天（知识库即培训资料） |
| **质量** | 答案一致、引用可追溯、政策更新上传即生效、7×24 可用 |

---

## 五、支撑材料索引

| 材料 | 文件 |
|---|---|
| 四场景压力测试报告（含瓶颈定位与优化建议） | `压测报告.md` |
| 安全审查报告（敏感信息/注入/配置泄露/认证授权） | `安全审查报告.md` |
| 单元测试报告（41 用例 + 覆盖率） | `测试报告.md` |
| 系统架构图（本文档第二章的独立版本） | `项目架构图.md` |
| 项目 README（部署/配置/生产化路线） | `README.md` |
