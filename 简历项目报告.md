# 简历项目报告 · 电商 RAG 知识库智能问答系统

> 使用说明：简历中的「项目经历」通常写 1 条（标准版），空间紧张用精简版；
> 「面试延伸」部分供面试官追问时口头展开，不必写进简历。

---

## 一、简历项目经历（标准版，可直接粘贴）

**电商 RAG 知识库智能问答系统** ｜ 独立开发 ｜ 2026.03 – 2026.09

- **项目简介**：基于 LangChain + LangGraph 构建的企业级 RAG 知识库问答系统，面向电商商品咨询场景，支持多格式文档知识库管理、流式问答与答案溯源（回答自动标注并展示知识库引用片段），后端 FastAPI + Vue3 全栈实现，含用户认证、多用户多会话、运营统计看板等完整功能闭环。

- **核心工作与成果**：
  - 使用 LangGraph 状态图编排 RAG 流水线（query 改写 → 向量+BM25 混合检索 RRF 融合 → 重排序 → 相关度门槛 → 带引用流式生成 → 滚动摘要记忆），实现多轮指代消解与「检索不到就确定性拒答、不编造」的可靠性设计；51 题金标测试集上 **recall@5 达 100%**，重排序 top1 准确率 100%，无关问题拒答正确率 100%。
  - 设计并实现**两级语义缓存**（精确文本匹配 + 查询向量余弦相似度，key 含会话摘要哈希实现记忆感知），重复提问响应从 4.6s 降至 **83ms**（零 LLM 调用），结合 token 埋点估算缓存节省成本并呈现在统计看板。
  - 实现 SSE 流式输出（心跳保活、断连检测、已生成内容落库）+ 全文异步摄入流水线（5 种文档格式解析、中文分块、批量向量化、后台任务进度轮询），首 token 延迟、p50/p95 延迟等指标全链路埋点，支撑性能分析与运营统计（ECharts 看板）。
  - 实现多用户会话隔离与安全认证：JWT access/refresh 双 token 旋转吊销、bcrypt 密码哈希、接口限流、管理员 RBAC 双端权限校验（后端依赖注入 + 前端路由守卫）。
  - 排查并解决 chromadb 原生扩展在 Python 3.14/Windows 下稳定段错误的问题（最小复现 + 定位到 hnswlib），按预案自研 numpy 内存矩阵向量检索（SQLite 持久化 float32 向量，万级 chunk 检索 <10ms），并实现模型网关不可用时的流式降级容错；编写 41 个自动化测试用例（含 LangGraph 全图 Fake 模型走查、RRF 公式验证、SSE 端到端断言）。

- **技术栈**：Python / FastAPI / LangChain / LangGraph / Vue3 / Element Plus / SQLite / Jieba / BM25 / RRF / 重排序模型 API / JWT / SSE / ECharts / pytest

---

## 二、精简版（简历空间紧张时）

**电商 RAG 知识库智能问答系统**（LangChain + LangGraph 全栈，独立开发）
- 用 LangGraph 编排 RAG 流水线（query 改写、向量+BM25 混合检索 RRF 融合、重排序、带引用流式生成），金标集 recall@5 = 100%，回答可溯源到具体文档片段。
- 设计两级语义缓存（精确 + 向量相似度、记忆感知），重复提问响应 4.6s → 83ms；SSE 流式输出 + 异步文档摄入流水线，全链路延迟/token 埋点与统计看板。
- 实现 JWT 双 token 认证、多用户会话隔离、限流与 RBAC 权限控制；编写 41 个自动化测试，全量通过。

---

## 三、面试延伸（追问时的展开素材）

**Q：RAG 流水线为什么用 LangGraph 编排？**
节点间有条件路由（有历史才改写、低分走拒答、长会话触发摘要），用状态图比硬编码 if-else 更清晰可测；多轮上下文由 API 层从数据库组装进初始 State，不用内存 checkpointer，天然满足「不同时间登录找回历史」的持久化需求。

**Q：混合检索怎么做的，为什么需要重排序？**
向量检索擅长语义但漏关键词，BM25（jieba 分词）擅长专有名词但不懂同义改写，双路召回后用 RRF（Reciprocal Rank Fusion）按排名融合；重排序模型再对候选精排一次——调试页面可直观看到同一问题「向量/BM25/RRF/重排」四层结果差异（如“充电时间”问题：向量第三名的移动电源文档被重排挤出，规格表被正确提入 top3）。

**Q：语义缓存如何避免多轮对话答错？**
缓存 key 不只含问题向量，还含会话历史摘要的哈希（记忆感知）；同文本精确匹配走毫秒级快速路径，换措辞走向量相似度（阈值用金标集校准）；TTL 24h + 命中计数统计价值。

**Q：遇到过最难的问题？**
chromadb 在 Python 3.14/Windows 下 upsert 稳定段错误——通过最小复现脚本定位到 chroma-hnswlib 原生扩展，与业务代码无关；按预案改自研 numpy 向量检索（成本：100 行代码；收益：零原生依赖、检索 <10ms），并保留与生产替换方案（Milvus/Qdrant）一致的接口层。另外处理过模型网关「部分模型仅支持非流式」的坑，实现流式自动降级，保证服务可用性。

**Q：性能数据怎么来的？**
每次问答在 request_logs 表记录分节点耗时（改写/检索/重排/生成）、首 token 延迟、token 用量与缓存命中标记；统计页实时算 p50/p95、缓存命中率、按模型 token 分布与成本折算（单价可配置），benchmark 脚本支持并发压测。

**Q：为什么用 SQLite 而不是 MySQL？**
演示环境零配置；代码层全部走 SQLAlchemy，迁移 MySQL/PostgreSQL 只改连接串。README 附「演示版 → 生产版」技术演进对照表（Milvus、ES、Celery、Redis、API 网关），体现选型是权衡而非将就。

---

## 四、按岗位方向的侧重点调整建议

| 目标岗位 | 简历中突出 |
|---|---|
| 后端/平台开发 | LangGraph 编排、双 token 认证、异步摄入线程池、SQLite WAL 并发、限流、埋点体系 |
| 算法/NLP/RAG 方向 | 混合检索+重排的实验对比（recall@5 100%）、金标集评估方法、拒答率设计、缓存阈值校准 |
| 前端方向 | SSE 流式消费状态机、markdown 引文上标自定义渲染（XSS 防护）、ECharts 看板、路由守卫 |
| 测试/质量方向 | 41 用例的分层策略：Fake 模型全图走查、RRF 公式单测、SSE 帧级断言、真实语料解析器测试 |

---

*数据来源：`scripts/eval_retrieval.py`（51 题金标集）、`scripts/benchmark.py`、`scripts/demo.py` 实测输出，均可复现。*
