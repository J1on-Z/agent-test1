# 压力测试报告 · 电商 RAG 知识库智能问答系统

> 测试时间：2026-09-10 ｜ 工具：Locust 2.x + 自研分析脚本 ｜ 目标：模拟 100 人同时使用

## 1. 测试环境与方法

### 1.1 环境

| 项 | 配置 |
|---|---|
| 服务器 | Windows 10 单机，Python 3.14，**uvicorn 单进程**（无多 worker） |
| 压测客户端 | Locust 与服务器**同机运行**（资源竞争，见 1.3 局限说明） |
| 数据库 | SQLite（WAL 模式，busy_timeout 15s，连接池 20+30） |
| 外部依赖 | 阿里云百炼（LLM qwen3.7-flash / embedding / rerank） |
| LLM 并发闸门 | `LLM_MAX_CONCURRENCY=3`（保护外部 API 配额） |
| 知识库 | 64 份多格式商品文档，70 个分块 |

### 1.2 测试设计

- **账号模型**：100 个独立用户（各持独立 access token），每个用户复用同一会话（模拟真实多轮使用）
- **问题池**：77 条普通商品问题 + 5 条热点问题；压测按 **80% / 20%** 抽取，热点问题用于触发语义缓存
- **SSE 计时正确性**：流式响应必须等整个流读完才算「回答完成」，因此**不使用 `stream=True`**（那会在响应头到达时即停止计时，严重低估耗时）
- **交叉验证**：Locust 客户端视角 + 服务端 `request_logs` 埋点（分节点耗时、TTFT、缓存标记）双向印证

### 1.3 局限说明（如实声明）

1. **客户端与服务端同机**：100 个 Locust 协程与 uvicorn 争抢同一台机器的 CPU 与网络栈，绝对数值偏悲观（部分失败源于客户端连接层，而非服务端）。
2. **单进程部署**：Windows 下 uvicorn 单进程；生产 Linux 多 worker / 多实例会显著改变结论。
3. **外部 API 依赖**：LLM/embedding/rerank 为阿里云服务，其配额与网络状况影响结果。

---

## 2. 四场景结果

| 场景 | 并发模型 | 限流配置 | 总请求 | 失败率 | RPS | p50 | p95 |
|---|---|---|---|---|---|---|---|
| **S1 常态负载** | 100 用户，思考间隔 5-15s，持续 5 分钟 | 放宽 | 2,693 | **0%** | 9.02 | 210ms | 3.6s |
| **S1' 中并发突发** | 30 用户瞬间并发，1 分钟 | 放宽 | 306 | **0%** | 5.16 | 170ms | 21s |
| **S4 容量上限** | 100 用户瞬间并发，2 分钟 | 放宽 | 1,702 | **95%** | 14.34 | 2.1s | 13s |
| **S2/S3 限流保护** | 100 用户瞬间并发，1 分钟 | 生产值 120/min | 850 | **95.6%** | 14.25 | 10s | 13s |

**核心结论**：

- ✅ **100 人在线常态使用完全稳定**：5 分钟 2,693 次请求 0 失败，9 RPS 持续吞吐
- ⚠️ **100 人瞬间同时并发会击穿单机容量**：成功率约 5%
- ⚠️ **容量边界在 30～100 并发之间**：30 用户突发 0 失败，100 用户突发断崖式恶化
- ❌ **限流未能起到保护作用**（原因见 3.4）

---

## 3. 瓶颈分析（逐层剥离）

压测的价值在于**逐层剥掉瓶颈、暴露下一层**。以下是本次压测依次暴露的 4 层问题：

### 3.1 第一层：LLM 并发信号量未接入（已修复）

**现象**：代码中定义了 `_llm_semaphore`（保护 DashScope 配额）但从未被任何调用点使用。
**风险**：100 并发会直接冲击外部 API，触发供应商侧熔断。
**修复**：改为 `asyncio.Semaphore`（threading 版会阻塞事件循环），接入 generate / rewrite / 摘要三处 LLM 调用点。

### 3.2 第二层：连接池耗尽（已修复）

**现象**：S4 首轮出现 27 次 `QueuePool limit of size 5 overflow 10 reached`。
**根因**（两层叠加）：
1. 请求级 DB session（`Depends(get_db)`）**跨越整个 LLM 生成期（20-30 秒）持有连接**，100 并发 × 长时间持有 > 连接池上限；
2. 只读查询（记忆、语义缓存）执行后未结束事务，连接无法归还。

**修复过程中的一次失败尝试（有价值的教训）**：先用 `db.rollback()` 释放连接，结果 SQLAlchemy 回滚会 **expire 所有 ORM 对象**，后续访问 `conversation.id` 触发隐式 lazy load → 在异步上下文抛 `MissingGreenlet`，反而导致 100% 失败。

**最终修复**：流式问答的**所有 DB 操作改用独立短生命周期 session**（连接毫秒级归还），彻底不依赖请求级 session。修复后同样场景错误率从 50% 降到 0%（30 用户突发验证）。

### 3.3 第三层：SQLite 写锁串行化（架构性瓶颈，本次未修复）

这是 100 并发下**真正的天花板**。服务端埋点数据揭示了关键矛盾：

| 指标 | 数值 | 说明 |
|---|---|---|
| `generate` 节点耗时 | **p50 1,088 ms** | LLM 生成本身很快 |
| 端到端 **TTFT** | **p50 20,352 ms** | 但请求要等 20 秒才出第一个字 |
| 缓存命中率 | 96.4% | 绝大多数请求根本没调 LLM |

**生成只要 1 秒，请求却等 20 秒**——等待不在 LLM，而在**数据库写入**：

每个问答请求需 5 次写库（写用户消息、更新会话计数、写助手消息、写 request_logs、写语义缓存）。100 并发 = **500 次写操作争抢 SQLite 的单写者锁**（WAL 模式下写事务必须串行）。每次写入含 fsync 开销，累积成 20 秒的排队延迟。

旁证：压测期间出现 25 次 `database is locked`，且**会话列表接口**（纯读）p50 从正常 12ms 恶化到 1,300ms。

### 3.4 第四层：限流设计只防单用户、不防群体冲击

S2/S3 场景**没有出现预期的 429**。原因：slowapi 的 `key_func` 按用户维度计数（`user:{id}`），`RATE_LIMIT_GLOBAL=120/minute` 实际是**每用户**限额，100 个用户各自计数互不影响。

**结论**：现有设计能防单账号滥用，**无法保护系统免受大量用户同时涌入**。建议增加系统级全局限流（如全局 QPS 上限 + 排队深度控制）。

### 3.5 补充：客户端连接层失败

S4/S2 中约 24% 的失败是 `HTTP 0`（连接层），其中部分源于**客户端与服务端同机竞争**（见 1.3）。这部分不应全部归因于服务端。

---

## 4. 与 request_logs 的交叉验证

| 视角 | S4 期间数据 | 结论 |
|---|---|---|
| Locust 客户端 | /api/chat 1,454 请求全部标记失败 | 客户端等不到完整响应 |
| 服务端 request_logs | **实际完成 1,298 次请求** | 服务端仍在处理，但耗时超过客户端容忍度 |

**服务端分节点耗时（p50）**：`retrieve 1ms` → `rerank 227ms` → `generate 1,088ms` → **端到端 20s+**

**意义**：检索与生成本身的性能是健康的（毫秒~秒级），瓶颈完全在**请求调度与数据库写入**环节。这为优化指明了精确方向。

---

## 5. 优化建议（按性价比排序）

| 优先级 | 措施 | 预期效果 |
|---|---|---|
| ⭐⭐⭐ | **数据库换 PostgreSQL / MySQL** | 真正的并发写入，消除 20s 写锁排队（第三层瓶颈） |
| ⭐⭐⭐ | **Linux 多进程部署**（uvicorn --workers N / gunicorn） | 突破单进程 CPU 天花板（实测后端进程 CPU 峰值 80%） |
| ⭐⭐ | **压测机与服务端分离** | 消除同机资源竞争，获得真实容量数据 |
| ⭐⭐ | **写入优化**：批量写入、request_logs 异步批量落库、减少每请求写次数 | 直接降低写锁争用 |
| ⭐⭐ | **背压机制**：LLM 排队深度超阈值时快速返回 503 + Retry-After | 避免雪崩式排队（当前请求会一直等到客户端超时） |
| ⭐ | **全局系统级限流**（令牌桶 + 队列深度） | 补齐 3.4 的保护缺口 |
| ⭐ | **提升 LLM 并发配额**（当前闸门 3） | 提高峰值吞吐，需同步评估供应商配额与成本 |
| ⭐ | **热点问题预缓存**（系统启动时预热 Top-N 问题） | 进一步放大 96% 缓存命中率的收益 |

---

## 6. 成本记录

| 项 | 数值 |
|---|---|
| 压测期间累计 token | 51,962 |
| 真实 LLM 调用次数 | 47 次（96.4% 被语义缓存拦截） |
| 估算费用 | **< ¥0.1**（qwen3.7-flash 单价低，缓存大幅降低成本） |

> 缓存机制在压测中同时验证了其**性能价值**（96.4% 拦截率）与**成本价值**。

---

## 7. 复现方式

```bash
cd backend
# 1. 生成 100 个压测账号 + token 池 + 问题池
python scripts/prepare_load_users.py

# 2. 常态场景（100 用户 / 5 分钟）
LOAD_WAIT=normal .venv/Scripts/locust.exe -f scripts/locustfile.py \
    --host http://127.0.0.1:8000 -u 100 -r 10 -t 5m --headless --csv data/load_s1

# 3. 突发场景（100 用户瞬间并发）
LOAD_WAIT=burst .venv/Scripts/locust.exe -f scripts/locustfile.py \
    --host http://127.0.0.1:8000 -u 100 -r 100 -t 2m --headless --csv data/load_s4

# 4. 资源采样（另开终端）
python scripts/sample_resources.py --duration 700 --out data/load_res.csv

# 5. 服务端视角分析（分节点耗时 / TTFT / 缓存命中）
python scripts/export_load_report.py --minutes 30 --csv data/load_detail.csv
```

---

## 8. 结论摘要

1. **100 人在线常态使用：系统稳定**（0 失败，9 RPS，p50 210ms）——满足设计预期。
2. **100 人瞬间并发：超出单机容量**（成功率 ~5%），但**瓶颈定位精确**：不是检索、不是生成，而是 SQLite 写锁串行化 + 单进程部署 + 客户端同机竞争。
3. **压测发现并修复了 2 个真实缺陷**（LLM 信号量未接入、请求级 session 跨生成期持有连接），修复过程本身也产出一个反模式教训（rollback 导致 ORM 对象过期）。
4. **遗留 2 项架构建议**（数据库升级、背压机制）已列入第 5 节优化清单。
5. **语义缓存在压测中验证有效**：96.4% 拦截率，显著降低延迟与成本。
